name: Test parselglossy

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: [3.6, 3.7, 3.8]
        exclude:
          - os: macos-latest
            node: 3.8

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip poetry
        poetry install -v
    - name: Test with pytest
      run: |
        poetry run python -m pytest --cov=parselglossy --cov-report=xml --doctest-modules tests/
        poetry run python -m pytest --flake8 parselglossy
        poetry run python -m pytest --black parselglossy
        poetry run python -m pytest --mypy parselglossy
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v1
